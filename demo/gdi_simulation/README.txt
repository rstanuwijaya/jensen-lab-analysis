Run the following commands to install the dependencies (for Windows)

python -m pip install opencv-python
python -m pip install --upgrade Pillow
python -m pip install matplotlib
python -m pip install numpy
