function ret = capture(FrameTime, AcqTime)
    disp("capture!")

    % OK = pf_getLibraryStatus;
    % if(OK)
    %     % Close the library if it is already open. The scripts will load it
    %     % when needed.
    %     pf_close
    % end

    % [pf32, alias, report] = pf_open;
    % pf_setExposure(pf32, FrameTime);

    ret = 0
end